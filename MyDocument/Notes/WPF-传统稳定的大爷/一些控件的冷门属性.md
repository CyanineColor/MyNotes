1.TextBox控制输入只能为英文输入法
InputMethod.IsInputMethodEnabled=“False”
**是否允许“输入法引擎（IME, Input Method Editor）”参与键盘输入**。

- 设为 `True`（默认）  
    系统 IME 处于活动状态，用户可以切中文/日文/韩文等输入法，出现候选窗口。
    
- 设为 `False`  
    完全关闭当前控件的 IME，键盘消息直接当成“裸字符”送进控件——  
    中文输入法无法弹出，候选窗消失，相当于强制“纯英文键盘”模式。